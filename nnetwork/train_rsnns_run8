#!/bin/bash
#@ job_name = train_rsnns8
#@ initialdir = .
#@ output = train_rsnns8_%j.out
#@ error = train_rsnns8_%j.err
#@ total_tasks = 1
#@ cpus_per_task = 1
#@ wall_clock_limit = 24:00:00

R-3.1.3/bin/Rscript train_rsnns8.R
